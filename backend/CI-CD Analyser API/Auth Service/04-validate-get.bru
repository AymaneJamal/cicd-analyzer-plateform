meta {
  name: Validate Token (GET)
  type: http
  seq: 4
}

post {
  url: http://localhost:5004/api/auth/validate
  body: json
  auth: none
}

body:json {
  {
  "token": "<JWT_TOKEN>",
    "requestMethod": "GET"
  }
}
